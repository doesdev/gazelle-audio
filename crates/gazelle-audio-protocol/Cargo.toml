[package]
name = "gazelle-audio-protocol"
version = "1.2.0"
edition = "2021"
rust-version.workspace = true
description = "Protocol-correct reimplementation of the Antelope Audio Synergy Core USB wire protocol"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
